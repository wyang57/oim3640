Dictionaries map keys to values.

Counting pattern uses dictionaries.

info is a dictionary.

info['shortName'] gives a string.

info['currentPrice'] gives a number.

APIs come later.

<!-- Review Questions -->

.split() makes a list of words.

'iPhone' in summary gives True/False.

Strings cannot be changed.

len(info) counts key–value pairs.

info has many keys.

<!-- Review Questions -->

sorted(tickers) returns a new list.

It does not change the original list.

sum(prices.values()) adds all prices.

'TSLA' in prices checks for a key.

tickers.append('GOOG') adds to list.

prices['GOOG'] = ... adds to dict.

You can build nested dictionaries.

Tuples use parentheses.

Tuples are immutable.

Tuples can be sliced.

You cannot assign to a tuple index.

a, b = b, a swaps values.

(3, 4) can unpack into x, y.

Looping over dict.items() unpacks pairs.

Functions can return tuples.

Unpacking assigns multiple variables.

min_max() returns two values.

Lists cannot be dict keys.

Strings can be keys but messy.

Tuples make clean multi-part keys.

zip() pairs items.

Looping over zip gives two variables.

dict(zip(names, prices)) builds a dictionary.

Sets remove duplicates.

Sets are unordered.

set(tickers) gives unique tickers.

len(set(...)) counts unique items.

& gives intersection.

| gives union.

- gives difference.

Membership uses in.

Sets use hashing.

Lists scan each element.

Sets are much faster for lookup.

List is square brackets
Append is to add a string

Tuples are immutable

If it returns multiple values, it is a tuple.

Lists → append()
Sets → add()

Use sets when you only want one of each item.

Choosing Data Structures
Type	Mutable?	Ordered?	Access by	Duplicates?
str	No	Yes	index	Yes
list	Yes	Yes	index	Yes
tuple	No	Yes	index	Yes
dict	Yes	Yes	key	Keys: No
set	Yes	No	membership	No



When to Use What?
list: ordered collection, may have duplicates
Student grades, stock price history
tuple: fixed data that shouldn't change
GPS coordinates, returning multiple values
dict: look up values by a meaningful key
Stock prices by ticker, word frequency counts
set: unique items, fast membership testing
Unique words in a document, valid usernames


🙋 Which Data Structure?
You're building a music app:

Your favorites playlist (add, remove, reorder)
All artists a user has ever listened to (no repeats)
Song title → number of times played
A single song record: ('Bohemian Rhapsody', 'Queen', 1975)
